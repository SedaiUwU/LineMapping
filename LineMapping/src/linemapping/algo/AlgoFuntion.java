package linemapping.algo;

public class AlgoFuntion {
	/*new instance of LineMapObject()
	 *
	 * File 1 new instance of LineMapObject()
	 * file 2 new isntance of LineMapObject()
	 * 
	 * ittereate through lines to check between both hashes if they are same by ittereateing both hashes in a loop 
	 * 
	 * 				if they are the same
	 * 					add them to a list that documents that lines are identical
	 * 				if they are different 
	 *					mark the line in the according file to be different
	 *					check neighboring lines to see in both files around 10 lines to check top and bottom
	 *						mark as new line or deleted line in file 2
	 *						make Candidates based on search
	 *				
	 *
	 * well fuck i just realised im reinventing the The Hunt-McIlroy algorithm and wasteing time so gonna refference that later instead of making sudocode
	 */
	
	
}

